function ojo() {
   
    
    if (Password.type == 'password') {
        Password.type = 'text';
    } else if (Password.type == 'text') {
        Password.type = 'password';
    }
}
function ir(){

    window.location = "main.html"
    
}